spring:
  datasource:
    url: jdbc:h2:mem:clsnet-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  h2:
    console:
      enabled: false

clsnet:
  threads:
    ingestion: 1
    matching: 1
    netting: 1
    settlement: 1
